# Copy to .env and adjust values before running docker compose.
# SQL Server SA password (min 8 chars, incl. upper/lower/digit/symbol).
DB_PASSWORD=Senha123!

# JWT signing key (min 32 chars).
JWT_KEY=my-super-secret-key-at-least-32-characters-long
